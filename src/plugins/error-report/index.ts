/* eslint-disable @typescript-eslint/no-unused-vars */
import { App, } from 'vue'

export default function(app: App) {
  // app.config.warnHandler = (msg, ins, trace) => {
  //   // console.warn(msg + trace)
  // }
}
